/**
 * JustLaw Application Configuration
 * Backend URL Management
 */

const CONFIG = {
    // Development URL (Localhost)
    // API_BASE_URL: 'http://localhost:8000',

    // Production URL (Render Backend)
    // Sizin göndereceğiniz URL buraya gelecek. Örn: https://justlaw-api.onrender.com
    API_BASE_URL: 'https://justlaw-api.onrender.com', // Placeholder

    // Firebase Hosting URL (Frontend)
    FRONTEND_URL: 'https://justlawai.web.app'
};

export default CONFIG;
